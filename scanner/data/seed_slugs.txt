# ATS board slugs discovered by SEARCH get inserted into Postgres automatically.
# Do NOT seed mega-corp career boards (Notion/Stripe/OpenAI/…) — those invert the funnel.
# Format: ats:slug — optional hand-curated *early-stage* boards only.
#
# Example (uncomment if you have a known early hiring board):
# ashby:some-seed-startup
